{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "timeout": 60,
            "command": "command -v ccc >/dev/null && test -d \"${CLAUDE_PROJECT_DIR}/.cocoindex_code\" && ! printf '%s' \"${COCOINDEX_CODE_INDEXING_ROLE:-}\" | grep -qi '^[[:space:]]*member[[:space:]]*$' && (cd \"${CLAUDE_PROJECT_DIR}\" && ccc index) 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
